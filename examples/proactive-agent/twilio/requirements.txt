# Pinned for the v1.0 (May 19, 2026) proactive-agent example.
# Mirrors examples/twilio/requirements.txt (the underlying adapter)
# and adds the OpenAI Realtime client deps that AISDRBridge needs.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.30,<1.0
attenlabs-saa>=0.3.0,<1
numpy>=1.24
twilio>=9.0,<10.0
python-dotenv>=1.0.0
websockets>=12
openai>=1.0
