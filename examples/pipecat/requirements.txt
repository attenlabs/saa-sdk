# SAA × Pipecat reference adapter, pinned, runnable, end-to-end.
# Verified against pipecat-ai 1.x and attenlabs-saa 1.0 (the May 19, 2026
# launch release).
attenlabs-saa>=0.3.0,<1
pipecat-ai[cartesia,daily,deepgram,openai,silero,webrtc]>=1.0.0,<2.0.0

# .env loading, standard across pipecat samples.
python-dotenv>=1.0.0

# Used by pipecat for structured logs (also imported in this sample).
loguru>=0.7.0

# Audio resampling + buffers. Pinned to the same major as pipecat-ai's
# transitive numpy to avoid a divergent install.
numpy>=2.0.0

# Optional: enables `feed_video` upstream encoding for non-JPEG image
# frames (Pipecat ships some transports as RGB). Skip if your transport
# already produces JPEG.
Pillow>=10.0.0

# Test deps, install separately if you run tests/.
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
